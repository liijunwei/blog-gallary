#!/bin/bash

hexo clean  &&
hexo deploy &&
ssh webuser@xiaoli "cd /srv/www/blog-gallary &&
                    git fetch         &&
                    git checkout main &&
                    git pull"

open_tag=$1
[[ $open_tag != "" ]] && open http://blog.bxzy.top
